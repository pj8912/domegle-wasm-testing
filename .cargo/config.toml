# getrandom 0.3 needs an explicit backend on wasm32-unknown-unknown; without
# this the build fails at link time with "the wasm*-unknown-unknown targets are
# not supported by default".
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
